# NotebookLM API and Telegram Bot Dependencies

# Core dependencies
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# NotebookLM MCP CLI (from local clone)
# Install from the cloned repository
./notebooklm-mcp-cli

# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Telegram Bot
python-telegram-bot>=21.0

# HTTP client (required by notebooklm-mcp-cli)
httpx>=0.27.0

# Optional: Redis for rate limiting and caching
# redis>=5.0.0

# Development dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
black>=24.0.0
ruff>=0.1.0
mypy>=1.0.0
